[project]
name = "ai-info-radar"
version = "0.1.0"
description = "Local AI information gate for source-aware alerts and digests."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
